# PhD Manuscript

## Building with a Makefile

You can build this Latex project using the provided Makefile that uses by default `latexmk`.

```bash
make
```

Using another Latex compiler can be done (with no guarantee of success):

```bash
make TEXC=my_compiler
```
